[project]
name = "api"
version = "0.1.0"
description = "FastAPI HTTP layer — read-only endpoints serving the dashboard"
requires-python = ">=3.11"
dependencies = [
  "core",
  "fastapi>=0.115",
  "uvicorn[standard]>=0.32",
  "python-json-logger>=2.0",
]

[tool.uv.sources]
core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/api"]
