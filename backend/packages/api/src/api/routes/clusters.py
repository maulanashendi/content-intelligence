# Cluster endpoints (all read-only):
#   GET /api/v1/clusters/morning       — top 10 for Maulana's morning view
#   GET /api/v1/clusters/{cluster_id}  — detail with member articles
#   GET /api/v1/clusters/deferred      — desk head's deferred-topics view
#
# GSC metrics are NEVER returned in any response (see docs/constraints.md).
