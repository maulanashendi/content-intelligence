# coverage_score computation.
# Counts competitor (rss) articles vs internal (Tempo) articles in the cluster.
# Also derives an internal_underperformed signal from article_gsc_metric — read
# only here, never returned via API (see docs/decisions.md D7).
