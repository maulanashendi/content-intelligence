include:
  - path: backend/docker-compose.yml

services:
  frontend:
    build:
      context: frontend
      dockerfile: Dockerfile
    environment:
      VITE_BACKEND_URL: http://api:8000
    ports:
      - "5173:5173"
    depends_on:
      - api
