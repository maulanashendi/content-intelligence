// Flat ESLint config for the workspace.
// Should: extend @typescript-eslint recommended, react-hooks rules,
// enforce no cross-package deep imports (no "@ei-fe/*/src/*"),
// forbid window/document outside useEffect (custom rule or convention),
// integrate with prettier.

export default []
