// Vite config for @ei-fe/app.
// Plugins: @vitejs/plugin-react.
// Resolve aliases mirroring tsconfig.base.json paths so Vite agrees with TS.
// Build target: modern browsers (es2022).
// Server proxy /api/v1 → http://localhost:8000 in dev for cross-origin avoidance.
