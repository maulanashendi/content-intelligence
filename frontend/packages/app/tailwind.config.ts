// Tailwind v4 config for the SPA.
// Extends preset from @ei-fe/ui/tailwind.preset.
// content globs include packages/{ui,features}/src/**/*.{ts,tsx}
// so Tailwind sees classes from sibling packages too.
