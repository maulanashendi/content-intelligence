// Entry point. Validate env (@ei-fe/core/env triggers on import).
// ReactDOM.createRoot(document.getElementById("root")).render(<App />)
// Wraps <App /> in <Providers />.
// Imports ./styles/globals.css once.
