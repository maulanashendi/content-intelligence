// Domain types shared across packages.
// Recommendation enum: "trending" | "worth_writing" | "saturated"
//   (mirrors backend insight_recommendation in schema.dbml).
// Other shared discriminators that don't belong to a specific package.
// Do NOT put API response shapes here — those live in @ei-fe/api/generated.ts.
