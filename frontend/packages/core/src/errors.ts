// ApiError class used by @ei-fe/api client.
// Fields: status (number), message (string), requestId (string | undefined).
// Helper isApiError(e) for type narrowing.
// Helper toUserMessage(e) that maps ApiError → Bahasa Indonesia copy
// for ErrorState display.
