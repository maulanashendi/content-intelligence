// ClusterTable — presentational table for the morning view.
// Columns: cluster label, velocity (VelocityBar), members,
// competitor_count, sample_headline, recommendation Badge.
// Props: clusters: Cluster[], onRowClick(id).
// No data fetching here.
