// DeferredTable — Morning columns plus days_deferred ("3 hari trending").
// Sorted by trend_velocity desc (BE-side).
// Visual emphasis on days_deferred: high count = stronger color
// per token palette (tokens.ts).
