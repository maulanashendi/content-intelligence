// ClusterDetailView — drill-in view for one cluster.
// Props: clusterId.
// Calls useClusterDetail(clusterId).
// Renders ClusterHeader + ArticleList. States handled like MorningView.
// 404 → EmptyState directing back to /morning.
