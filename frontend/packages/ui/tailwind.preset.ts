// Tailwind v4 preset shared by @ei-fe/app.
// Import tokens from @ei-fe/core/tokens and feed theme.extend:
//   colors (recommendation palette + neutrals), spacing scale,
//   fontFamily (Geist, Source Serif 4, JetBrains Mono),
//   fontSize, borderRadius, boxShadow.
// Export as default. Consumed via `presets: [preset]` in
// @ei-fe/app/tailwind.config.ts.
