// StatusBar shown above the page-head.
// Displays: last cluster_run timestamp, refetching indicator, optional
// degraded-health banner (from /health endpoint, surfaced through TanStack Query).
// Pure presentational — receives data via props from app shell.
