// EmptyState component.
// Props: icon (from icons.ts), title, description, action (optional).
// Used when an endpoint returns 0 items, or when a 404 cluster is requested.
// Bahasa Indonesia copy passed via props — component is generic.
